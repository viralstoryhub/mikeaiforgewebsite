# Application
NODE_ENV=development
PORT=3000
API_PORT=5000
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:5000

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/mikeaiforge
REDIS_URL=redis://localhost:6379

# Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d
REFRESH_TOKEN_SECRET=your-refresh-token-secret
REFRESH_TOKEN_EXPIRES_IN=30d

# Google Gemini AI
GEMINI_API_KEY=your-gemini-api-key

# Stripe Payment
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRO_PRICE_ID=price_...

# Email Service (SendGrid or Resend)
EMAIL_SERVICE=sendgrid
SENDGRID_API_KEY=your-sendgrid-api-key
FROM_EMAIL=noreply@mikesaiforge.com

# Analytics
# Frontend tracking (used by client-side analytics libraries like gtag)
# Set your client-side Measurement ID (GA4) to enable pageview/event tracking from the browser.
# NOTE: VITE_GA_MEASUREMENT_ID is for client-side tracking (frontend). The Google Analytics Data API variables below are for backend access to view analytics in the admin dashboard.
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Legacy or alternative key kept for compatibility with older codepaths.
GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

MIXPANEL_TOKEN=your-mixpanel-token

# Google Analytics Data API (for viewing analytics in admin dashboard)
# To set up:
# 1. Go to Google Cloud Console (console.cloud.google.com)
# 2. Create a new project or select existing
# 3. Enable Google Analytics Data API
# 4. Create a service account with Analytics Viewer role
# 5. Download JSON credentials and save to project root
# 6. In GA4, add service account email as a Viewer
# 7. Set GOOGLE_ANALYTICS_PROPERTY_ID to your GA4 property ID
GOOGLE_ANALYTICS_PROPERTY_ID=123456789
GOOGLE_ANALYTICS_CREDENTIALS_PATH=./google-analytics-credentials.json
# Optional: For Universal Analytics (if still in use)
GOOGLE_ANALYTICS_VIEW_ID=

# Error Tracking
SENTRY_DSN=https://...@sentry.io/...

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# News Aggregation
# Comma-separated list of RSS feed URLs for AI news aggregation
# Popular AI News Sources - Add or remove feeds as needed
NEWS_RSS_FEEDS=https://feeds.feedburner.com/venturebeat/SZYF,https://techcrunch.com/tag/artificial-intelligence/feed/,https://www.artificialintelligence-news.com/feed/,https://www.theverge.com/rss/ai-artificial-intelligence/index.xml
# How often to fetch news (cron format or duration like "6h", "30m")
# Default: "0 */6 * * *" (every 6 hours)
NEWS_FETCH_INTERVAL=0 */6 * * *
# Optional: Cache duration for RSS feeds (e.g., "5m", "10m", "1h")
NEWS_RSS_CACHE_TTL=5m
# Optional: Delay between RSS feed requests in milliseconds (to avoid rate limiting)
NEWS_RSS_REQUEST_DELAY_MS=1000

# Forum Settings
# Maximum number of characters allowed in a thread's content
FORUM_MAX_THREAD_LENGTH=10000
# Maximum number of characters allowed in an individual forum post
FORUM_MAX_POST_LENGTH=5000
# Number of posts returned per page when paginating thread replies
FORUM_POSTS_PER_PAGE=20

# Content Moderation
# Set to "true" to enable AI-powered content moderation for forum content
ENABLE_CONTENT_MODERATION=false
# API key for the external content moderation provider (required when moderation is enabled)
MODERATION_API_KEY=

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# Session
SESSION_SECRET=your-session-secret

# Frontend Environment Variables
# API Configuration - Points to the backend API (must include /api path)
# For local dev: http://localhost:5001/api
# For production: https://mikeaiforge-backend.onrender.com/api
VITE_API_BASE_URL=https://mikeaiforge-backend.onrender.com/api
